// RenderTextureUpdateMode - Constants for render texture update modes
// Use with RenderTexture::create() and RenderTexture::setUpdateMode()

public class RenderTextureUpdateMode {
    public static int EVERY_FRAME = 0;
    public static int ON_DEMAND = 1;
    public static int FIXED_INTERVAL = 2;
}
